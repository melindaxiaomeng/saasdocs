# 网盟 SaaS 平台 - 产品文档

> 版本：V3.0 | 最后更新：2026-04-02

---

## 目录

1. [平台概述](#1-平台概述)
2. [导航结构](#2-导航结构)
3. [功能模块详解](#3-功能模块详解)
4. [核心工作流](#4-核心工作流)
5. [数据指标说明](#5-数据指标说明)
6. [API 接口](#6-api-接口)

---

## 1. 平台概述

### 1.1 产品定位
这是一个 **广告联盟/网盟后台管理系统 (Ad Network Admin Platform)**，用于管理广告主（Advertiser）、流量主（Publisher）和广告系列（Campaign）的全流程运营。

### 1.2 核心角色
| 角色 | 说明 |
|------|------|
| **Advertiser（广告主）** | 投放广告的客户，如 Nike、Adidas |
| **Publisher（流量主）** | 提供流量的渠道，如 AppLovin、Unity Ads |
| **Campaign（广告系列）** | 广告投放计划，关联广告主和流量主 |
| **AM（Account Manager）** | 客户经理，负责广告主关系维护 |
| **PM（Publisher Manager）** | 渠道经理，负责流量主关系维护 |

### 1.3 技术栈
- **前端框架**：React 18 + React Router v6
- **UI 样式**：Tailwind CSS
- **图表库**：Recharts + ECharts
- **图标**：Lucide React
- **日期处理**：date-fns
- **HTTP 客户端**：Axios

---

## 2. 导航结构

### 2.1 侧边栏菜单

```
├── Overview（总览）
├── Workplace（工作区）
│   ├── Campaign Center（广告系列中心）
│   └── Partner Hub（合作伙伴中心）
├── Report Center（报表中心）
│   ├── Internal Performance Hub（内部业绩报表）
│   ├── Quality & Insight Center（质量洞察）
│   ├── Market Intelligence（市场情报）
│   └── Special Labs（专项实验室）
├── Resources（资源）
│   ├── Campaign Label（广告标签）
│   ├── Unified Account Vault（统一账户库）
│   └── Audiences Hub（受众中心）
├── Tools & Settings（工具与设置）
│   ├── Traffic Policy Center（流量策略中心）
│   ├── Mapping Workspace（映射工作区）
│   └── Global Settings（全局设置）
├── Finance（财务）
└── Logs & Debug（日志与调试）
    ├── Logs（日志）
    └── Debug（调试）
```

### 2.2 路由映射

| 路径 | 页面组件 | 说明 |
|------|---------|------|
| `/overview` | Overview | 总览页面 |
| `/workplace/campaign` | CampaignCenter | 广告系列中心 |
| `/workplace/partner` | PartnerHub | 合作伙伴中心 |
| `/report/performance` | ReportCenter | 内部业绩报表 |
| `/report/quality` | QualityInsightCenter | 质量洞察 |
| `/report/intelligence` | ReportCenter | 市场情报 |
| `/report/labs` | ReportCenter | 专项实验室 |
| `/resources/label` | CampaignLabel | 广告标签 |
| `/resources/vault` | UnifiedAccountVault | 统一账户库 |
| `/resources/audiences` | AudiencesHub | 受众中心 |
| `/tools/traffic` | TrafficPolicyCenter | 流量策略中心 |
| `/tools/mapping` | MappingWorkspace | 映射工作区 |
| `/tools/global` | GlobalSettings | 全局设置 |
| `/finance` | Finance | 财务报表 |
| `/logs/postback` | Logs | Postback 日志 |
| `/logs/debug` | Debug | 调试工具 |

---

## 3. 功能模块详解

### 3.1 Workplace（工作区）

#### 3.1.1 Campaign Center（广告系列中心）
**文件**：`src/pages/CampaignCenter.jsx`

**核心功能**：
- Campaign 列表管理（创建、编辑、删除、复制）
- 状态管理（Active / Paused / Stopped）
- 标签管理（Hot / Top / New）
- 批量操作（切换状态、Blo Publisher、Block Publisher）

**筛选条件**：
| 字段 | 类型 | 说明 |
|------|------|------|
| Advertiser | 多选下拉 | 广告主筛选 |
| Status | 单选 | Active / Paused / Stopped |
| Label | 多选 | Hot / Top / New |
| Device | 单选 | ALL / Android / iPhone |
| Country | 多选 | 国家筛选 |
| AM | 多选 | 客户经理筛选 |
| Publisher | 多选 | 流量主筛选 |

**核心指标**：
- ID、Name、Advertiser、Weget（收入）、Clicks、Conversions、Cap（配额）、Status、Update Time

**批量操作**：
1. 切换状态（Active ↔ Paused）
2. Blo Publisher（屏蔽）
3. Block Publisher（拉黑）
4. 复制 Campaign
5. 复制 ID
6. 复制推单信息

---

#### 3.1.2 Partner Hub（合作伙伴中心）
**文件**：`src/pages/PartnerHub.jsx`

**核心功能**：
- Advertiser（广告主）管理
- Publisher（流量主）管理

**Advertiser 功能**：
- 卡片式展示
- 详情编辑
- 状态切换（Active / Inactive）
- Email 快速编辑
- Weget Min/Max 编辑
- API 链接配置

**Publisher 功能**：
- 卡片式展示
- Link（全局规则）
- Settings（设置）
- Status 切换

**可编辑字段**：
- Email
- Weget Min / Weget Max

---

### 3.2 Report Center（报表中心）

#### 3.2.1 Internal Performance Hub（内部业绩报表）
**文件**：`src/pages/ReportCenter.jsx`

**视图模式**：
| 模式 | 说明 |
|------|------|
| Standard | 全维度报表 |
| Comparison | 对比模式 |
| Ranking | Top 排行 |

**维度选项**：
- Date、Hour、Advertiser、Publisher、Campaign、Package、Country、OS、Channel、T Campaign Id

**指标分类**：

| 分类 | 指标 |
|------|------|
| 数字指标 | Impressions、Clicks、Conversions、Postback Conv、Revenue、Payout、Profit、Average Price |
| 比例指标 | Margin%、CR% |
| 专项指标 | Reject Count、Ecpc |

---

#### 3.2.2 Quality & Insight Center（质量洞察）
**文件**：`src/pages/QualityInsightCenter.jsx`

- 流量质量分析
- 作弊检测
- CVR 异常监控

---

### 3.3 Tools & Settings（工具与设置）

#### 3.3.1 Traffic Policy Center（流量策略中心）
**文件**：`src/pages/TrafficPolicyCenter.jsx`

**策略模式**：
| 模式 | 说明 |
|------|------|
| mix | 混合模式 |
| replace | 替换模式 |

**触发类型**：
| 触发类型 | 说明 |
|----------|------|
| click | 点击触发（Click Count） |
| event | 事件触发（Event Name、Event Count） |
| random | 随机触发 |

**配置项**：
- Advertiser 选择
- Campaign 选择
- Publisher 选择
- Channel 选择
- 替换 Publisher
- 替换 Channel

**Policy Simulator（策略模拟器）**：
- 输入：Click ID（可选）、Publisher（必填）、Channel（可选）
- 按 P0→P3 优先级顺序遍历策略
- 输出：R Publisher、R Channel
- 展示匹配过程

---

#### 3.3.2 Mapping Workspace（映射工作区）
**文件**：`src/pages/MappingWorkspace.jsx`

**两个 Tab**：

**Tab 1: PKG/Appname（包名映射）**
| 字段 | 说明 |
|------|------|
| Package Name | 包名 |
| App Name | 应用名称 |
| Platform | 平台（iOS/Android） |
| Publisher | 流量主 |
| Channel | 渠道 |
| Advertiser | 广告主 |
| Remarks | 备注 |

**Tab 2: Event Name（事件映射）**
| 字段 | 说明 |
|------|------|
| Campaign | 广告系列 |
| Publisher | 流量主 |
| Mapping Event Name | 映射事件名 |
| Status | 状态（active/inactive） |

**事件分类**：
- Revenue（收入）
- Engagement（参与）
- Ad（广告）
- Acquisition（获取）
- Retention（留存）

---

#### 3.3.3 Global Settings（全局设置）
**文件**：`src/pages/GlobalSettings.jsx`

**Tab 1: Alert Logs（预警日志）**
| 字段 | 说明 |
|------|------|
| Publisher | 流量主 |
| Campaign | 广告系列 |
| Warning Type | 预警类型 |
| Current CVR | 当前 CVR |
| Threshold | 阈值 |
| Clicks | 点击数 |
| Conversions | 转化数 |
| Revenue | 收入 |
| Timestamp | 时间戳 |

**预警类型**：
- CVR Low（转化率过低）
- No Click（无点击）
- Spike（异常峰值）

**Tab 2: Publisher Config（渠道配置）**
- Publisher-specific Config
- CVR Limit Min/Max
- Click Threshold
- Conversion Threshold
- Revenue Threshold
- Use Global Config 开关

**Tab 3: Global Setting（全局设置）**
| 配置项 | 说明 |
|--------|------|
| Default Currency | 默认货币 |
| Timezone | 时区 |
| Global Cap | 全局配额 |
| Postback IP Whitelist | Postback IP 白名单 |

---

### 3.4 Finance（财务）
**文件**：`src/pages/Finance.jsx`

**筛选条件**：
- Date Range（月份选择：本月、上月、Q1）
- Campaign ID
- Third Part ID
- PackageName
- Country
- Advertiser（多选）
- Publisher（多选）
- AM（多选）
- PM（多选）
- A Status（Pending/Confirmed）
- P Status
- OS
- Discrepancy Rate > X%

**核心字段**：
| 字段 | 说明 |
|------|------|
| Revenue | 收入 |
| Scrub Revenue | 扣除收入 |
| C Revenue | 结算收入（Revenue - Scrub） |
| Payout | 支出 |
| Scrub | 扣除 |
| Cover | 补偿 |
| C Payout | 结算支出（Payout - Scrub + Cover） |
| Conversions | 转化数（前端） |
| PB Conv | Postback 转化（后端） |
| S Conv | Scrub 转化 |
| Reason | 拒绝原因 |

**状态确认**：
- A Status：Advertiser 确认状态
- P Status：Publisher 确认状态

**拒绝原因分类**：
- Fraud（欺诈）
- CTIT limit（点击安装时间过短）
- Post-Attribution（后归因）

**功能**：
- 盈亏着色（Revenue < Payout 红色高亮）
- 转化差异着色
- 上传对账单
- 账单预览与导出

---

### 3.5 Logs & Debug（日志与调试）

#### 3.5.1 Logs（日志）
**文件**：`src/pages/Logs.jsx`

**四个 Tab**：

| Tab | 说明 | 字段 |
|-----|------|------|
| Postback Log | Postback 日志 | Click ID、Campaign、Advertiser、Publisher、Event、Status、Delay(H)、Received At |
| Event Postback Log | 事件 Postback | Event Name、Revenue、Currency |
| PM Postback Log | PM Postback | PM Publisher、Original Publisher |
| PM Event Postback Log | PM 事件 Postback | - |

**筛选条件**：
- Date Range（今天、昨天、最近三天/七天/三十天、本周、上周、本月、上月）
- Click ID / Keyword 搜索
- Status 下拉
- Country / Advertiser / Publisher 多选

**功能**：
- 行展开查看详情
- Active Filters 显示
- Download 导出

---

#### 3.5.2 Debug（调试）
**文件**：`src/pages/Debug.jsx`

**三个 Tab**：

**Tab 1: Error Report（错误报表）**
| 字段 | 说明 |
|------|------|
| Error ID | 错误ID |
| Type | 类型（Postback Failed/Invalid Parameter/Rate Limited） |
| Message | 错误消息 |
| Click ID | 点击ID |
| Reason | 原因 |

**错误聚合分组**：
- Reason / Advertiser / Publisher / Country / Campaign

**Tab 2: Debug API（API 调试）**
- 固定模板 URL
- Publisher Id / Campaign Id 输入
- Preview URL 预览
- 响应结果复制

**Tab 3: Query Task（查询任务）**
| 字段 | 说明 |
|------|------|
| Task Name | 任务名称 |
| Create Time | 创建时间 |
| Filters | 筛选条件摘要 |
| Status | 状态（running/success/failed） |
| Progress | 进度 |

**状态操作**：
- Running：[Cancel]
- Success：[Download] [Delete]
- Failed：[Retry] [Log]

---

### 3.6 Resources（资源）

#### 3.6.1 Campaign Label（广告标签）
**文件**：`src/pages/CampaignLabel.jsx`

- 标签分类管理
- 标签颜色配置
- 标签与 Campaign 关联

#### 3.6.2 Unified Account Vault（统一账户库）
**文件**：`src/pages/UnifiedAccountVault.jsx`

- 账户统一管理
- 账户信息存储
- 账户共享与权限

#### 3.6.3 Audiences Hub（受众中心）
**文件**：`src/pages/AudiencesHub.jsx`

- 受众分组
- 受众画像
- 受众分析

---

### 3.7 Overview（总览）
**文件**：`src/pages/Overview.jsx`

- 今日关键指标
- 趋势图表
- 快捷入口
- 最近活动

---

## 4. 核心工作流

### 4.1 Campaign 创建与发布流程

```
1. Partner Hub 创建/编辑 Advertiser
         ↓
2. Partner Hub 创建/编辑 Publisher
         ↓
3. Campaign Center 新建 Campaign
         ↓
   - 关联 Advertiser
   - 设置预算和配额（Cap）
   - 配置目标国家/设备
         ↓
4. Mapping Workspace 配置事件映射
         ↓
5. Traffic Policy Center 配置流量策略（可选）
         ↓
6. Campaign Center 激活 Campaign（Active）
```

### 4.2 财务对账流程

```
1. Finance 页面筛选月份和条件
         ↓
2. 查看 Revenue vs Payout 差异
         ↓
3. 分析 Conversions vs PB Conv 差异
         ↓
4. 查看 Reason 分类（Fraud/CTIT/Post-Attribution）
         ↓
5. 上传对账单（如有外部数据）
         ↓
6. A Status 确认（Advertiser 确认）
         ↓
7. P Status 确认（Publisher 确认）
         ↓
8. 双方确认后生成账单
         ↓
9. 导出 PDF 账单
```

### 4.3 流量策略配置流程

```
1. Traffic Policy Center 新建策略
         ↓
2. 选择模式（mix / replace）
         ↓
3. 配置触发条件
   - Advertiser 选择
   - Campaign 选择
   - Publisher 选择
   - Channel 选择
   - 触发类型（click/event/random）
         ↓
4. 配置替换规则
   - Replace Publisher
   - Replace Channel
         ↓
5. 设置优先级（P0-P3）
         ↓
6. 激活策略
         ↓
7. 使用 Policy Simulator 验证
```

### 4.4 日志排查流程

```
1. Logs 页面选择日志类型
         ↓
2. 设置 Date Range 和筛选条件
         ↓
3. 查看 Postback 日志详情
         ↓
4. 如发现异常 → Debug 页面
         ↓
5. Error Report 查看错误聚合
         ↓
6. Debug API 验证 API 响应
         ↓
7. Query Task 查询详细数据
```

---

## 5. 数据指标说明

### 5.1 核心指标

| 指标 | 说明 | 计算公式 |
|------|------|----------|
| Revenue | 收入（我方从广告主收取） | - |
| Payout | 支出（我方向流量主支付） | - |
| Profit | 利润 | Revenue - Payout |
| Margin% | 利润率 | Profit / Revenue × 100% |
| Clicks | 点击数 | - |
| Conversions | 转化数（前端归因） | - |
| PB Conv | Postback 转化（后端归因） | - |
| CR% | 转化率 | Conversions / Clicks × 100% |
| Ecpc | 有效 CPC | Revenue / Clicks |
| Average Price | 平均价格 | Revenue / Conversions |

### 5.2 财务调整指标

| 指标 | 说明 | 计算公式 |
|------|------|----------|
| Scrub Revenue | 扣除收入 | - |
| C Revenue | 结算收入 | Revenue - Scrub Revenue |
| Scrub | 扣除支出 | - |
| Cover | 补偿支出 | - |
| C Payout | 结算支出 | Payout - Scrub + Cover |

### 5.3 差异分析

| 指标 | 说明 |
|------|------|
| Discrepancy Rate | 转化差异率：(Conversions - PB Conv) / PB Conv × 100% |
| Revenue Discrepancy | 收入差异率：(C Revenue - C Payout) / C Revenue × 100% |

---

## 6. API 接口

### 6.1 API 模块

**文件路径**：`src/api/`

| 文件 | 功能 |
|------|------|
| `auth.js` | 认证 API（login, logout, refresh） |
| `users.js` | 用户 CRUD API |
| `advertisers.js` | 广告主 API |
| `departments.js` | 部门 CRUD API |
| `positions.js` | 职务 CRUD API |
| `menus.js` | 菜单 API |
| `permissions.js` | 权限 API |
| `menuCollections.js` | 菜单集合 API |
| `selectors.js` | 下拉选项 API |
| `resourceManagement.js` | 资源管理 API |
| `userRelations.js` | 用户关系 API |

### 6.2 API 配置

**后端地址**：`http://10.7.95.31:8000`

**配置文件**：`vite.config.js`（Vite 代理配置）

---

## 附录

### A. 状态码说明

| 状态 | 说明 |
|------|------|
| Active | 激活状态 |
| Paused | 暂停状态 |
| Stopped | 停止状态 |
| Inactive | 未激活 |
| Pending | 待确认 |
| Confirmed | 已确认 |

### B. 快捷键

| 快捷键 | 功能 |
|--------|------|
| Enter | 确认输入 |
| Esc | 取消操作 |
| Ctrl+C | 复制 |
| Ctrl+V | 粘贴 |

### C. 日期格式

- 日期显示：`YYYY-MM-DD`
- 时间显示：`YYYY-MM-DD HH:mm:ss`
- 快捷日期：今天、昨天、最近7天、本周、上周、本月、上月

---

*文档生成时间：2026-04-02*
